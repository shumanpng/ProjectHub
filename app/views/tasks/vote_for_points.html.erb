<%= form_tag vote_for_points_path do %>

  <h3> How many points do you think should be assigned to this task?</h3>

  <p>
    <strong>Points:</strong>
    <%= @task.points %>
  </p>

  <%= stylesheet_link_tag "tasks.scss" %>


  <!-- <%= form_for(@task) do |f| %> -->
    <!-- <div class = "bgcolor">
      <div class="field">
        <%= f.radio_button :points, '1 (Easy)' %>
        <%= label_tag("1 (Easy)") %><br>
        <%= f.radio_button(:points, "2 (Medium)") %>
        <%= label_tag("2 (Medium)") %><br>
        <%= f.radio_button(:points, "3 (Hard)") %>
        <%= label_tag("3 (Hard)") %><br>
      </div>
      <div class="actions">
        <%= f.submit 'Vote'%> -->
      <!-- </div>
    </div> -->
  <!-- <% end %> -->

<% options = [1,2,3]%>
<% options.each do |points| %>
  <%= link_to positive_task_path(points), method: :put do%>
    <button type="button" class="btn btn-success" aria-label="Left Align">
      <%= link_to positive_task_path(@task), method: :put do %>
        Upvote
        <%= @task.get_upvotes.size %>
      <% end %>

      <%= link_to negative_task_path(@task), method: :put do %>
      Downvote
      <%= @task.get_downvotes.size %>
      <% end %>

    <p><%=points%></p>
    </button>

<!-- if the person has alreadt voted, we should break out of loop -->
    <%if @task.votes_for.up.by_type(@user).size == 1%>
    <% break %>

    <% end %>


  <% end %>
<%end%>

<!-- <%= link_to positive_task_path(@task), method: :put do %>
Upvote
<%= @task.get_upvotes.size %>
<% end %>

<%= link_to negative_task_path(@task), method: :put do %>
Downvote
<%= @task.get_downvotes.size %>
<% end %> -->

</br>
<%= @task.votes_for.up.by_type(User).voters%></br>
<%= @task.votes_for.up.by_type(User).size%></br>

  <%= link_to 'Show', task_path %> |
  <%= link_to 'Back', edit_task_path %>


<%end%>
