<%= form_for(@active_user) do |f| %>
  <% if @active_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@active_user.errors.count, "error") %> prohibited this active_user from being saved:</h2>

      <ul>
      <% @active_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :token %><br>
    <%= f.text_field :token %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
