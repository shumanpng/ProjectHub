<div class="container">
    <h1>Editing Group</h1>

    <%= render 'form' %>
    <br>
  	<h2>Group Members</h2>
    <table>
	    <thead>
	      <tr>
	        <th>Name</th>
	      </tr>
	    </thead>

	    <tbody>
	      <%= @members.each do |member| %>
	       	<tr>
	          <td><%= member.name %></td>
	          <td><%= link_to 'Destroy', group_membership_path(:id => member.id,:token => @user_login.token), method: :delete, data: { confirm: 'Are you sure?' } %></td>
	      	</tr>
	      <% end %>
	  	</tbody>
	</table>


    <%= link_to 'Back', groups_path(:token => @user_login.token) %>
</div>
