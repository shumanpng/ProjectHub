<%= form_for(@group_request) do |f| %>
  <% if @group_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_request.errors.count, "error") %> prohibited this group_request from being saved:</h2>

      <ul>
      <% @group_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.number_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
